# /public/images/gallery

Drop event photos here, then list their filenames in the `galleryImages`
array at the top of `app/gallery/page.tsx`. The page shows a "no photos yet"
placeholder until you do.

Recommended: consistent aspect ratio (square works best with the current
grid), reasonably compressed (~200-500KB each) for fast page loads.
