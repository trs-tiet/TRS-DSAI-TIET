# /public/docs

Drop the final brochure PDF here as:

    trs-dsai-brochure.pdf

It's already referenced by `site.brochureUrl` in `data/site.ts` and linked from
the "Download brochure" button on the Home page — no code changes needed once
the file is in place.
